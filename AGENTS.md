# SaydianAIOS 协作开发规则

## 原型与正式开发

1. 用户确认过的原型是正式功能的唯一实现基准。
2. 正式开发前，先把原型中的入口、字段、默认值、状态、按钮、权限、数据来源和异常提示列成核对清单。
3. 正式代码必须逐项完成核对清单，不能只实现部分页面或只接后端数据。
4. 旧版本与已确认原型冲突时，删除、替换或停用旧实现，不保留两套并行入口。
5. 原型中的模拟数据必须替换为真实接口；无法接入时明确标记，不能用静态数据伪装成功。
6. 每次完成后先提供仅展示本次改动的本地预览，并进行类型检查、构建和必要测试。
7. 用户明确确认可以部署后才能推送和部署；部署优先使用既有 OSS 中转流程，失败时停止并报告。

## 多人协作

1. 修改代码前先同步远端最新主分支。
2. 每项改动从最新主分支建立独立分支，通过 Pull Request 合并。
3. 禁止强制推送，禁止覆盖同事或用户尚未合并的改动。
