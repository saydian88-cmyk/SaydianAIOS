# 协作任务清单

负责人约定：

- `laiyuanhuia`：开发协作者。
- `赛电负责人`：账号、凭据、业务规则和最终验收。

## P0：先完成

### [ ] ONBOARD-01 接受仓库邀请并建立开发环境

负责人：`laiyuanhuia`

交付：

- 接受 `SaydianAIOS` 私有仓库邀请。
- 克隆仓库并从 `main` 创建个人任务分支。
- 完成本地启动、TypeScript、测试和构建。

验收：

```powershell
pnpm typecheck
pnpm test
pnpm build
```

全部通过，无密钥或 `.env` 进入 Git。

### [ ] DOUYIN-01 完成抖音账号授权闭环

开发负责人：`laiyuanhuia`  
业务配合：`赛电负责人`

步骤：

1. 赛电负责人使用目标抖音账号扫码授权。
2. 检查 OAuth 回调是否保存 `open_id`、访问令牌和刷新令牌。
3. 检查授权、解除授权 Webhook 是否写入审计记录。
4. 验证令牌自动续期状态。
5. 后台显示授权账号、有效期、最近成功时间和错误。

验收：

- 抖音连接状态为“正常”。
- `PlatformAccount` 存在真实授权账号。
- Webhook 验证和授权事件均返回成功。
- 不在日志、前端或数据库明文字段显示密钥。

### [ ] CLOUD-AI-01 验证 IMS＋百炼媒体链路

开发负责人：`laiyuanhuia`  
云权限配合：`赛电负责人`

用例：

1. 一张产品图完成 OCR、型号识别、分类和评分。
2. 一个 60 秒视频完成代理、截图、转写、切段、分类和评分。
3. 一个 10 分钟视频在 30 分钟内完成，或返回明确失败原因。
4. 同一文件重复提交不重复建素材和云任务。
5. IMS 或百炼失败时可在后台重试。

验收：

- OSS、IMS回调、百炼视觉、转写和文本生成分别显示最后成功时间。
- 片段可追溯原视频和时间范围。
- 失败任务保留错误、重试次数和云任务编号。

## P1：数据采集与生产

### [ ] VIRAL-01 确定四平台爆款采集方案

方案负责人：`laiyuanhuia`  
采购决策：`赛电负责人`

平台：

```text
抖音
TikTok
小红书
视频号
```

工作：

- 对每个平台比较官方接口、第三方数据服务、CSV导入和链接补录。
- 明确可获取字段、频率、费用、稳定性和下载能力。
- 先提交决策表，不在来源未确定前编写专用爬虫。
- 官方抖音接口只处理赛电授权账号；同行爆款使用第三方或导入。

验收：

- 每个平台有一个确定的主方案和一个降级方案。
- 未配置来源仍可使用CSV和链接补录。
- 外部视频进入参考池，不进入商用素材池。

### [ ] VIRAL-02 接入选定采集源

负责人：`laiyuanhuia`

工作：

- 使用现有 `ViralCollectorService` 适配选定数据源。
- 保存内容ID、链接、作者、发布时间和可获取指标。
- 有下载权限时进入 OSS 并提交云端分析。
- 高分视频生成待确认仿拍任务。

验收：

- 每日串行限速采集。
- 重复内容不会重复建档。
- 采集失败不影响商城、中台和日报。
- 日报显示平台采集数、AI分析数、失败数和仿拍任务数。

### [ ] CONTENT-01 连续验证视频与软文日生产

负责人：`laiyuanhuia`

每日输出：

- 三个视频候选方向。
- 一套主视频执行包。
- 一个软文母题及多平台版本。
- 推荐素材编号、素材缺口和补拍建议。

验收：

- 连续运行七天。
- 所有产品事实、FAQ和证据可追溯。
- 型号无法确认时进入待审核。
- 外部参考只生成结构和仿拍建议。

## P2：效果反馈

### [ ] PERFORMANCE-01 内容效果回流素材评分

负责人：`laiyuanhuia`

工作：

- 保存发布后 1、6、24、72 小时和 7、30 日数据快照。
- 关联发布员工、素材版本、平台账号和内容任务。
- 将真实效果写入发布后评分。
- 符合条件的素材进入黄金资产候选。

验收：

- 同一素材不同版本可对比。
- 播放量、互动率、点击和订单不混为同一指标。
- 无法获取的字段显示“未获取”，不按零值统计。

## 提交格式

每个任务使用一个分支：

```text
codex/douyin-oauth
codex/cloud-ai-validation
codex/viral-collector
codex/content-production
codex/performance-feedback
```

Pull Request 必须写明：

```text
目标
修改文件
测试结果
线上验证
未完成项
回滚方式
```

